# No configuration needed.
#
# "Should I Buy It?" runs entirely in the browser — the product classifier and
# the verdict engine are local, deterministic JavaScript. There is no API key,
# no backend, and no network request at runtime.
#
# This file is kept only so the repo layout stays familiar. You can delete it.
